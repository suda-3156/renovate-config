{
  "name": "renovate-config",
  "scripts": {
    "validate": "for file in *.json*; do if [[ \"$file\" == \"package.json\" ]]; then continue; fi; echo \"Validate: $file\"; RENOVATE_CONFIG_FILE=$file renovate-config-validator || exit 1; done"
  },
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "renovate": "^42.25.4"
  }
}
